---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---
<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			:root {
				--accent: #00b8ff;
				--accent-dark: #0077cc;
				--accent-glow: rgba(0, 184, 255, 0.15);
				--black: 10, 12, 16;
				--gray: 150, 155, 165;
				--gray-light: 40, 45, 55;
				--gray-dark: 25, 30, 40;
				--background: 15, 20, 30;
				--text: 220, 225, 235;
				--neon-glow: 0 0 5px rgba(0, 184, 255, 0.2), 
					0 0 20px rgba(0, 184, 255, 0.1);
				--box-shadow: 0 2px 6px rgba(0, 0, 0, 0.4),
					0 8px 24px rgba(0, 0, 0, 0.2);
			}
			
			body {
				background: rgb(var(--background));
				background-image: 
					radial-gradient(rgba(var(--accent-glow), 0.15) 1px, transparent 1px),
					radial-gradient(rgba(var(--accent-glow), 0.1) 1px, transparent 1px);
				background-size: 50px 50px, 25px 25px;
				background-position: 0 0, 25px 25px;
				color: rgb(var(--text));
			}
			
			main {
				max-width: 960px;
				margin: 0 auto;
				padding: 2rem;
				line-height: 1.6;
			}
			
			h1 {
				margin-bottom: 1.5rem;
				font-size: 3em;
				color: white;
				text-shadow: var(--neon-glow);
				background: linear-gradient(90deg, #fff, var(--accent));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
			}
			
			h2, h3 {
				color: var(--accent);
				text-shadow: var(--neon-glow);
			}
			
			.services {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
				gap: 2rem;
				margin: 2.5rem 0;
			}
			
			.service-card {
				background-color: rgba(var(--gray-dark), 0.4);
				border-radius: 8px;
				padding: 1.5rem;
				box-shadow: var(--box-shadow);
				transition: transform 0.3s ease, box-shadow 0.3s ease;
				border: 1px solid rgba(var(--gray), 0.1);
			}
			
			.service-card:hover {
				transform: translateY(-5px);
				box-shadow: var(--box-shadow), var(--neon-glow);
			}
			
			.service-card h3 {
				color: var(--accent);
				margin-top: 0;
			}
			
			.icon {
				font-size: 2rem;
				margin-bottom: 1rem;
				color: var(--accent);
			}
			
			.contact-section {
				background-color: rgba(var(--gray-dark), 0.4);
				padding: 2rem;
				border-radius: 8px;
				margin-top: 3rem;
				border: 1px solid rgba(var(--gray), 0.1);
			}
			
			.contact-form {
				display: grid;
				gap: 1rem;
				max-width: 600px;
			}
			
			.form-group {
				display: flex;
				flex-direction: column;
			}
			
			label {
				margin-bottom: 0.5rem;
				font-weight: 500;
				color: rgb(var(--text));
			}
			
			input, textarea {
				padding: 0.75rem;
				background: rgba(var(--black), 0.5);
				border: 1px solid rgba(var(--gray), 0.3);
				border-radius: 4px;
				font-family: inherit;
				color: rgb(var(--text));
			}
			
			input:focus, textarea:focus {
				outline: none;
				border-color: var(--accent);
				box-shadow: 0 0 0 2px var(--accent-glow);
			}
			
			button {
				background-color: var(--accent);
				color: rgb(var(--black));
				padding: 0.75rem 1.5rem;
				border: none;
				border-radius: 4px;
				font-weight: 600;
				cursor: pointer;
				transition: all 0.3s ease;
				margin-top: 1rem;
			}
			
			button:hover {
				background-color: white;
				box-shadow: var(--neon-glow);
			}
			
			/* Header styling overrides */
			:global(header) {
				background-color: rgba(var(--black), 0.7);
				backdrop-filter: blur(10px);
				border-bottom: 1px solid rgba(var(--gray), 0.2);
			}
			
			:global(header h2 a), :global(header nav a) {
				color: var(--accent) !important;
			}
			
			:global(header nav a:hover) {
				text-decoration: none;
				text-shadow: var(--neon-glow);
				color: white !important;
			}
			
			:global(.social-links svg) {
				color: var(--accent);
			}
			
			:global(.social-links a:hover svg) {
				color: white;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<h1>Professional IT Services</h1>
			<p>
				Welcome to my professional IT services. I provide reliable, efficient, and customized technology solutions to help your business thrive in today's digital environment.
			</p>
			
			<div class="services">
				<div class="service-card">
					<div class="icon">💻</div>
					<h3>Technical Support</h3>
					<p>Responsive troubleshooting and technical assistance for all your hardware and software needs, with minimal downtime.</p>
				</div>
				
				<div class="service-card">
					<div class="icon">🔒</div>
					<h3>Network Security</h3>
					<p>Comprehensive security solutions to protect your business data and infrastructure from evolving threats.</p>
				</div>
				
				<div class="service-card">
					<div class="icon">☁️</div>
					<h3>Cloud Solutions</h3>
					<p>Strategic cloud implementation and migration services to enhance accessibility and scalability.</p>
				</div>
			</div>
			
			<p>
				With years of experience in the IT industry, I deliver tailored solutions that align with your business objectives and technical requirements.
			</p>
			
			<section class="contact-section" id="contact">
				<h2>Contact Me</h2>
				<p>Let's discuss how I can help address your IT challenges. Fill out the form below, and I'll get back to you promptly.</p>
				
				<form class="contact-form" action="https://formspree.io/f/xjkyoqna" method="POST">
					<div class="form-group" id="contact-form">
						<label for="name">Name</label>
						<input type="text" id="name" name="name" required>
					</div>
					
					<div class="form-group">
						<label for="email">Email</label>
						<input type="email" id="email" name="email" required>
					</div>
					
					<div class="form-group">
						<label for="subject">Subject</label>
						<input type="text" id="subject" name="subject">
					</div>
					
					<div class="form-group">
						<label for="message">Message</label>
						<textarea id="message" name="message" rows="5" required></textarea>
					</div>
					
					<button type="submit">Send Message</button>
				</form>
			</section>
		</main>
		<Footer />
	</body>
</html>
